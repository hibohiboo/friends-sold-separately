get:
  operationId: getGyuttoHandUserById
  summary: ユーザ情報取得API
  description: ユーザIDを指定して、対応したユーザ情報を取得する
  parameters:
    - in: path
      name: id
      description: ユーザID
      required: true
      schema:
        type: string
  responses:
    200:
      description: 200 OK
      content:
        application/json:
          schema:
            $ref: '../components/gyutto-hand-user.yml#/components/schemas/DynamoGyuttoHandUserProfile'
